import { NgxReorderDirective } from './ngx-reorder.directive';

describe('NgxReorderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxReorderDirective();
    expect(directive).toBeTruthy();
  });
});
